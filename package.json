{
  "name": "creator-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "normalize-data": "node scripts/normalize-data.mjs",
    "build": "node scripts/build.mjs",
    "deploy": "wrangler deploy",
    "cf:whoami": "WRANGLER_HOME=.wrangler npx --cache .npm-cache wrangler@4 whoami",
    "preview": "wrangler dev"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "wrangler": "^4.94.0"
  }
}
